{
  "name": "jacks-stocks-backend",
  "version": "1.0.0",
  "description": "REST API for Jacks Stocks — an inventory management system with EOQ, ABC classification and reorder point (ROP) support.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --env-file ./config/.env --watch ./src/server.js",
    "start": "node --env-file ./config/.env ./src/server.js"
  },
  "keywords": ["inventory", "eoq", "express", "postgresql"],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.11.0",
    "pg": "^8.22.0"
  }
}
